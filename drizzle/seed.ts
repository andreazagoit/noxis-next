/**
 * Seed minimale: il gestionale è stato rimosso, non c'è più nulla da popolare.
 * L'utente admin si crea via sign-in (better-auth) e promozione manuale:
 *   UPDATE "user" SET role = 'admin' WHERE email = '...';
 */
console.log('Niente da seedare: il gestionale è stato rimosso. Vedi commento nel file.')
